<h1><%=currentUser.firstname%>'s Profile</h1>

<div class="row">
    <div class="col s4">
        <img src="<%=currentUser.pic%>" alt="profile pic">
    </div>
    <div>
        <p>
            <strong>Name:</strong> <%= currentUser.firstname%> <%=currentUser.lastname%>
        </p>
        <p>
            <strong>Birthday:</strong> <%=moment(currentUser.birthday).format('MMM Do[,] YYYY')%>
        </p>
        <p>
            <strong>Account created on: </strong><%=moment(currentUser.createdAt).format('MMM Do[,] YYYY')%>
        </p>
        <p><%=currentUser.bio%><p>
    </div>
</div>